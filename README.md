# gtd-assets

This orphan branch stores file attachments for gtd_mgmt issues.
Do not merge it into `main`. Each issue's files and manifest live under `issues/<issue_number>/`. The manifest.json in each issue directory is the source of truth for that issue's attachments — never inferred from comments.
